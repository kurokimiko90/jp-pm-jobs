"""各 stage の prompt テンプレートと閘門設定。

出力言語は繁体中文（読み手＝応募者本人）。JD 原文の引用のみ日本語のまま「」で囲む。
required_sections は prompt 内の見出しと**完全一致**させること
（ずれると Gate A が永遠に落ちる）。
"""

from __future__ import annotations

from .gates import GateSpec

_COMMON_RULES = """\
# 共通ルール（違反すると機械閘門で差し戻される）
1. 出力は繁体中文。JD 原文を引用するときだけ日本語のまま「」で囲む。
2. 会社固有の数字（売上・ユーザー数・シェア等）は、上の「確認済みの会社事実」に
   書かれているものだけ使う。無ければ {{要確認}} と書く。捏造は禁止。
3. 他社事例を数字つきで挙げてよいのは「増長案例庫」に載っている企業だけ。
   それ以外の企業名を出すときは数字を書かず、{{要出典確認}} を付ける。
4. 推測を事実として書かない。推測は「推測：」と明示する。
5. 見出しは指定されたものを**そのままの文字列**で使う（増減・改変しない）。
6. 冗長な前置き・末尾のまとめ文は書かない。表と箇条書きを優先する。
"""

# ============================================================
#  Stage 1: JD 解構
# ============================================================
DECOMPOSE_SECTIONS = [
    "## 1. 事業模型與收益結構",
    "## 2. 這個職位承擔的增長課題",
    "## 3. 增長槓桿候補",
    "## 4. 職責 → 所需技能對應表",
    "## 5. JD 未明說的隱含期待",
    "## 6. 成功的定義",
]

DECOMPOSE_PROMPT = """\
あなたは日本の SaaS / AI プロダクト業界に精通したグロース責任者です。
以下の求人票を「この仕事をどう遂行するか」の観点で解構してください。

{context}

# 規則ベースの事前タグ付け（参考・鵜呑みにせず JD 本文で検証すること）
{tags}

{common_rules}

# 出力（この 6 つの見出しを順に、そのままの文字列で使う）

## 1. 事業模型與收益結構
JD から読み取れる収益モデル（誰が / 何に / どう課金するか）、顧客セグメント、
販売モデル（セールス主導 / PLG / 混合）を推定し、根拠となる JD の記述を「」で引用する。
読み取れない項目は {{要確認}}。

## 2. 這個職位承擔的增長課題
この PdM が解くべき「本当の課題」を 3〜5 個。各項目に必ず：
- 課題の一文記述
- そう判断した JD 上の根拠（「」引用）
- 放置した場合に悪化する指標

## 3. 增長槓桿候補
獲客 / 啟動 / 留存 / 營收 / 擴張 / 效率 の 6 分類から、この職位が実際に触れる
槓桿を選び、表で整理する。列は：槓桿 / 為何是這個職位的責任 / 影響的指標 / 優先度（高中低）/ 判斷根據。

## 4. 職責 → 所需技能對應表
表形式。列は：JD 上的職責（「」引用）/ 對應技能 / 技能分類（探索・實驗・分析・協作・領域知識）/
熟練度要求（入門・實務・專家）/ 判斷根據。最低 8 行。

## 5. JD 未明說的隱含期待
JD には書かれていないが、この事業モデル・組織規模・業界慣行から確実に求められること。
各項目に「なぜそう言えるか」の推論を添える（推測は「推測：」と明示）。

## 6. 成功的定義
入職 6 ヶ月・12 ヶ月時点で「この人を採ってよかった」と言われる状態を、
測定可能な形で定義する。表：時點 / 成果物 / 判定指標 / 判定基準。
"""

DECOMPOSE_SPEC = GateSpec(
    required_sections=DECOMPOSE_SECTIONS,
    min_chars=1800, min_lines=45, require_table=True,
)

# ============================================================
#  Stage 2: 指標樹
# ============================================================
METRICS_SECTIONS = [
    "## 1. 北極星指標",
    "## 2. 輸入指標",
    "## 3. 護欄指標",
    "## 4. 指標定義書",
    "## 5. 計測缺口與補齊計畫",
]

METRICS_PROMPT = """\
あなたはプロダクトアナリティクスに強いグロース責任者です。
前段の JD 解構を踏まえ、この職位が最初に立てるべき指標体系を設計してください。

{context}

# 前段の成果物（JD 解構）
{prev_decompose}

# 指標フレームワーク（この骨格から選んで使う）
{frameworks}

{common_rules}
7. 指標は必ず「計算式」「対象期間」「除外条件」まで書く。曖昧な指標名だけの記述は差し戻し。

# 出力（この 5 つの見出しを順に、そのままの文字列で使う）

## 1. 北極星指標
候補を 2〜3 個挙げ、それぞれ：定義式 / 顧客價值與商業價值的兼顧理由 / 缺點。
最後に 1 つを推薦し、理由を 3 行以内で書く。

## 2. 輸入指標
推薦した北極星を分解した 3〜5 個。表：指標 / 計算式 / 為何能推動北極星 / 負責職能 / 資料來源。

## 3. 護欄指標
北極星を追うことで壊れうるものを守る指標。表：指標 / 守護對象 / 觸發閾值 / 觸發時的行動。
最低 3 行。AI プロダクトの場合は品質・コスト系を必ず含める。

## 4. 指標定義書
上で挙げた全指標の定義を 1 つの表に。列は：指標名 / 計算式 / 對象期間 / 除外條件 /
資料來源 / 現況值。現況値は不明なら {{要確認}}。

## 5. 計測缺口與補齊計畫
今の情報では計測できない指標を列挙し、それぞれに：必要な計測実装 / 想定工数 /
それが無い間の代替指標。
"""

METRICS_SPEC = GateSpec(
    required_sections=METRICS_SECTIONS,
    min_chars=1600, min_lines=40, require_table=True,
)

# ============================================================
#  Stage 3: 標竿研究
# ============================================================
BENCHMARK_SECTIONS = [
    "## 1. 可移植的案例",
    "## 2. 移植改造設計",
    "## 3. 不可移植的案例與理由",
    "## 4. 本職位可借鑑的共通機制",
]

BENCHMARK_PROMPT = """\
あなたはグロース事例の分析者です。優れたプロダクトが実際にどう成長を作ったかを、
この求人の文脈に翻訳してください。

{context}

# 前段の成果物（増長槓桿）
{prev_decompose}

# 増長案例庫（★数字つきで引用してよいのはこの中の企業だけ★）
{cases}

{common_rules}
7. 案例庫にある事例だけを「事例」として扱う。庫外の知識を使うときは
   企業名を出さず「業界では〜という手法がある」と一般化し、{{要出典確認}} を付ける。
8. 「結果」を書くときは案例庫の記述をそのまま使い、数字を盛らない。

# 出力（この 4 つの見出しを順に、そのままの文字列で使う）

## 1. 可移植的案例
案例庫から本求人に効く事例を 4〜6 件選び、それぞれ：
- 案例 ID と名前
- 為什麼與本職位相關（JD の記述と結びつける）
- 該案例的核心機制（1〜2 行）
- 出典（案例庫の source をそのまま）

## 2. 移植改造設計
上で選んだ各事例について、この会社の事業モデルにそのまま持ち込めない部分と、
どう作り変えるかを表で。列は：案例 / 原始做法 / 本社不能照搬的原因 / 改造後的做法 / 首次驗證方式。

## 3. 不可移植的案例與理由
案例庫の中で「一見効きそうだが本求人には合わない」ものを 2〜3 件、理由つきで。
これを書くことで判断基準が明確になる。

## 4. 本職位可借鑑的共通機制
個別事例ではなく、複数事例に共通する仕組み（例：価値到達の可視化、
実験の事前登録、コスト連動の価格）を 3〜5 個抽出し、
それぞれ「本求人で実装するとしたら何をするか」を 1 行で。
"""

BENCHMARK_SPEC = GateSpec(
    required_sections=BENCHMARK_SECTIONS,
    min_chars=1600, min_lines=40, require_table=True,
)

# ============================================================
#  Stage 4: 實施 playbook
# ============================================================
PLAYBOOK_SECTIONS = [
    "## 1. 槓桿別實施手順",
    "## 2. 前置條件與依賴",
    "## 3. 失敗模式與對策",
]

PLAYBOOK_PROMPT = """\
あなたは実行に強いグロース責任者です。前段で選んだ槓桿を、
明日から着手できる粒度の手順に落としてください。

{context}

# 前段の成果物
## JD 解構
{prev_decompose}
## 指標体系
{prev_metrics}
## 標竿研究
{prev_benchmark}

{common_rules}
7. 各手順には必ず「担当」「期間」「成果物」を書く（この 3 語を実際に使う）。
   書けない手順は粒度が粗すぎるので分割する。
8. 「〜を検討する」「〜を推進する」のような動詞は禁止。
   「〜のデータを抽出し、〜の表を作る」のように検証可能な動作で書く。

# 出力（この 3 つの見出しを順に、そのままの文字列で使う）

## 1. 槓桿別實施手順
前段で優先度「高」とした槓桿ごとに `### 槓桿名` の小見出しを立て、
その下に手順を 4〜7 ステップ。各ステップは箇条書きで：
- 動作（検証可能な動詞で）
- 担当：（役割名。自分でやるなら「本人」）
- 期間：（第 N 週 / N 日）
- 成果物：（ファイル名やレポート名）
- 完了判定：（何をもって次に進むか）

## 2. 前置條件與依賴
手順の実行に必要な、自分では用意できないもの（データアクセス権 / 分析基盤 /
他部署の協力 / 予算 / 意思決定権限）を表で。列は：前置條件 / 為何必要 /
誰能提供 / 沒有時的替代方案 / 取得時機。

## 3. 失敗模式與對策
この計画が失敗する現実的なパターンを 5 個以上。表：失敗模式 / 早期徵兆 /
預防措施 / 發生後的止損動作。
「リソース不足」のような一般論ではなく、この事業・この組織で起きうる形で書く。
"""

PLAYBOOK_SPEC = GateSpec(
    required_sections=PLAYBOOK_SECTIONS,
    min_chars=2000, min_lines=55, require_table=True,
    block_marker="###",
    block_required=[["担当", "負責", "擔當"], ["期間", "週", "日"], ["成果物", "產出", "成果"]],
)

# ============================================================
#  Stage 5: 運營節奏與報告
# ============================================================
CADENCE_SECTIONS = [
    "## 1. 30 日",
    "## 2. 60 日",
    "## 3. 90 日",
    "## 4. 91〜180 日",
    "## 5. 例行報告目錄",
    "## 6. 報告骨架",
]

CADENCE_PROMPT = """\
あなたは入職 180 日の立ち上がり設計を数多く見てきたグロース責任者です。
「各段階で何をやり、何のレポートを出すか」を確定してください。

{context}

# 前段の成果物
## 指標体系
{prev_metrics}
## 実施手順
{prev_playbook}

# 報告目錄（この骨格を土台に、本求人向けに取捨選択・改造する）
{reports}

{common_rules}
7. 各レポートには必ず「頻度」「読者」「用途」を書く（この 3 語を実際に使う）。
8. 活動量（面談 N 件）ではなく成果物で段階目標を書く。

# 出力（この 6 つの見出しを順に、そのままの文字列で使う）

## 1. 30 日
表：事項 / 具體動作 / 產出報告 / 判定基準。最低 5 行。
この段階の狙いを表の前に 2 行で書く。

## 2. 60 日
同じ構成。30 日で得た情報を前提にした内容にする（独立した別計画にしない）。

## 3. 90 日
同じ構成。ここで「最初の測定可能な成果」が出る設計にする。

## 4. 91〜180 日
同じ構成。仕組み化・横展開・次の四半期の仕込みを含める。

## 5. 例行報告目錄
定常運用に入ったあとのレポート一覧。表：報告名 / 頻度 / 読者 / 用途 / 輸入資料 / 所需工時。
最低 6 行。AI プロダクトなら品質・コスト系レポートを必ず含める。

## 6. 報告骨架
上の一覧のうち、最も重要な 3 つについて、そのまま使える見出し構成を書く。
各レポートは `### レポート名` の小見出しで始め、見出しリストと、
各見出しに何を書くかの 1 行説明を付ける。
"""

CADENCE_SPEC = GateSpec(
    required_sections=CADENCE_SECTIONS,
    min_chars=2200, min_lines=60, require_table=True,
    block_marker="###",
    block_required=[["頻度", "頻率"], ["読者", "讀者"], ["用途", "決策", "決定"]],
)

# ============================================================
#  Stage 6: 交叉驗證
# ============================================================
CROSSCHECK_SECTIONS = [
    "## 1. 三方交叉矩陣",
    "## 2. 可立即上手",
    "## 3. 需要補課",
    "## 4. 高風險項目",
    "## 5. 面試可用的具體說法",
]

CROSSCHECK_PROMPT = """\
あなたは採用側の視点も持つ厳しいレビュアーです。
「JD の要求」「標竿事例の打法」「応募者の実力」を突き合わせ、
どこまで本当にできるのかを冷静に判定してください。褒めるのが仕事ではありません。

{context}

# 前段の成果物
## 職責 → スキル対応
{prev_decompose}
## 標竿研究
{prev_benchmark}
## 実施手順
{prev_playbook}

{common_rules}
7. 応募者の経験は上の「応募者プロフィール」に書かれている事実だけを根拠にする。
   書かれていない経験を推定で足さない（これは面接で破綻する）。
8. 「高風險」を 1 件も出さない出力は不合格。必ず弱点を特定する。

# 出力（この 5 つの見出しを順に、そのままの文字列で使う）

## 1. 三方交叉矩陣
表。列は：JD 要求的技能 / 標竿做法所需的能力 / 應募者的對應經驗（プロフィールからの引用）/
判定（可立即上手・需補課・高風險）/ 判定根據。最低 10 行。

## 2. 可立即上手
判定が「可立即上手」の項目を、証拠となる具体的な経験とセットで。
面接で聞かれたときに出す実例を 1 つずつ添える。

## 3. 需要補課
不足しているが 90 日で埋められるもの。各項目：現況 / 目標水準 /
埋め方（具体的な行動）/ 検証方法。

## 4. 高風險項目
90 日では埋まらない、あるいは構造的に不利な点。各項目：
リスクの内容 / なぜ埋まらないか / 入職後の緩和策 / 面接で聞かれた場合の正直な答え方。
ここを甘く書くと入職後に破綻するので、遠慮なく書く。

## 5. 面試可用的具體說法
上記を踏まえ、「この仕事をどう進めますか」と聞かれたときの回答骨子を 3 パターン
（30 秒版 / 2 分版 / 深掘り対応版）。日本語の敬体で書く（ここだけ日本語）。
"""

CROSSCHECK_SPEC = GateSpec(
    required_sections=CROSSCHECK_SECTIONS,
    min_chars=1800, min_lines=45, require_table=True,
)

# ============================================================
#  Stage 7: 技能增長計畫
# ============================================================
SKILLPLAN_SECTIONS = [
    "## 1. 技能落差總表",
    "## 2. 90 日訓練計畫",
    "## 3. 每項技能的驗收證據",
    "## 4. 可立即動手的練習環境",
]

SKILLPLAN_PROMPT = """\
あなたは PdM のスキル開発を設計するメンターです。
前段で特定した不足を、実際に手を動かして埋める計画にしてください。

{context}

# 前段の成果物
## 交叉検証
{prev_crosscheck}
## 指標体系
{prev_metrics}

{common_rules}
7. 「本を読む」「勉強する」は成果物にならないので不可。
   必ず「何を作るか（成果物）」で書く。
8. 応募者が既に持っている資産（プロフィール記載の proof_projects や
   AI エンジニアリング経験）を練習台として活用する案を優先する。

# 出力（この 4 つの見出しを順に、そのままの文字列で使う）

## 1. 技能落差總表
表：技能 / 現況水準（入門・實務・專家）/ 目標水準 / 落差的具體內容 / 優先度。

## 2. 90 日訓練計畫
週単位。表：週次 / 對象技能 / 具體動作 / 產出物 / 所需時數。
週あたり合計 5〜8 時間を上限にする（在職・転職活動中の現実的な負荷）。

## 3. 每項技能的驗收證據
「できるようになった」と言える証拠。表：技能 / 驗收方式 / 合格基準 /
可放進職務経歴書的寫法。

## 4. 可立即動手的練習環境
手元にある資産（既存プロジェクト、公開データ、この求職パイプライン自体）を
使った練習の具体案を 3〜5 個。各案：練習內容 / 對應技能 / 產出物 / 預估時數。
"""

SKILLPLAN_SPEC = GateSpec(
    required_sections=SKILLPLAN_SECTIONS,
    min_chars=1400, min_lines=35, require_table=True,
)


STAGES: dict[str, dict] = {
    "decompose": {
        "file": "01_jd_decomposition.md",
        "title": "JD 解構 — 這份工作到底要做什麼",
        "prompt": DECOMPOSE_PROMPT,
        "spec": DECOMPOSE_SPEC,
        "deps": [],
        "timeout": 420,
        "needs_profile": False,
        # 成果物の冒頭に原 JD 全文を機械的に差し込む（LLM に書かせない＝改変ゼロ）
        "include_jd": True,
    },
    "metrics": {
        "file": "02_metric_tree.md",
        "title": "指標體系 — 用什麼衡量做得好不好",
        "prompt": METRICS_PROMPT,
        "spec": METRICS_SPEC,
        "deps": ["decompose"],
        "timeout": 420,
        "needs_profile": False,
    },
    "benchmark": {
        "file": "03_benchmark.md",
        "title": "標竿研究 — 優秀產品怎麼做到的",
        "prompt": BENCHMARK_PROMPT,
        "spec": BENCHMARK_SPEC,
        "deps": ["decompose"],
        "timeout": 420,
        "needs_profile": False,
    },
    "playbook": {
        "file": "04_playbook.md",
        "title": "實施手冊 — 具體怎麼做",
        "prompt": PLAYBOOK_PROMPT,
        "spec": PLAYBOOK_SPEC,
        "deps": ["decompose", "metrics", "benchmark"],
        "timeout": 600,
        "needs_profile": False,
    },
    "cadence": {
        "file": "05_operating_cadence.md",
        "title": "運營節奏 — 各階段各事項產出什麼報告",
        "prompt": CADENCE_PROMPT,
        "spec": CADENCE_SPEC,
        "deps": ["metrics", "playbook"],
        "timeout": 600,
        "needs_profile": False,
    },
    "crosscheck": {
        "file": "06_crosscheck.md",
        "title": "交叉驗證 — 我到底做不做得到",
        "prompt": CROSSCHECK_PROMPT,
        "spec": CROSSCHECK_SPEC,
        "deps": ["decompose", "benchmark", "playbook"],
        "timeout": 480,
        "needs_profile": True,
    },
    "skillplan": {
        "file": "07_skill_growth_plan.md",
        "title": "技能增長計畫 — 90 天怎麼補",
        "prompt": SKILLPLAN_PROMPT,
        "spec": SKILLPLAN_SPEC,
        "deps": ["crosscheck", "metrics"],
        "timeout": 420,
        "needs_profile": True,
    },
}

STAGE_ORDER = ["decompose", "metrics", "benchmark", "playbook", "cadence",
               "crosscheck", "skillplan"]

COMMON_RULES = _COMMON_RULES
